using System;
using Xamarin.Forms;

namespace Mais
{
    public class CustomEntry : Entry
    {
        public CustomEntry()
        {
            TextColor = Colors.EntryPlaceholderTextColor;
            BackgroundColor = Colors.EntryBackgroundColor;
        }
    }
}

